# opal-script-skills

> Portable AI-assistant skills for writing Opal (Minecraft utility client)
> JavaScript scripts, plus a dependency-free cross-AI installer. The skill
> teaches the real Opal GraalVM-JS scripting API so assistants stop inventing
> methods.

This is a small Node (ESM) package. The skill in `skills/opal-scripting/` is the
source of truth; everything else delivers it.

## Skill (source of truth)

- [SKILL.md](skills/opal-scripting/SKILL.md): the skill — script structure, the
  module/settings/event model, the renderer canvas and the color rule, palette
  command-palette views, dynamic islands, the default-deny sandbox model, and a
  common-mistakes list.
- [reference.md](skills/opal-scripting/reference.md): the module/settings/event
  model index, plus the `keys` table.
- [reference/core.md](skills/opal-scripting/reference/core.md): `client`,
  `notification`, `overlay`, `modules`, `mc`, `timer`, `ScriptList`.
- [reference/character.md](skills/opal-scripting/reference/character.md):
  `player`, `movement`, `rotation`, `inventory`, `mc.interactionManager`.
- [reference/world.md](skills/opal-scripting/reference/world.md): `world`,
  `esp`, the class globals (`BlockPos`, `Vec2f`, `Vec3d`, `Color`, hand
  constants), and the wrapper types the proxies return (`ScriptVec3`,
  `ScriptEntity`, `ScriptEffect`, `ScriptBox2D`, …).
- [reference/ui.md](skills/opal-scripting/reference/ui.md): `renderer`,
  `palette`.
- [palette-views.md](skills/opal-scripting/palette-views.md): a complete worked
  `palette.createView` example.

## Installer

- [bin/install.mjs](bin/install.mjs): `opal-skills add [--target <t>] [--dir <p>] [--all]`
  and `opal-skills list`. Renders the skill into per-assistant surfaces
  (claude-code, codex, gemini, copilot, cursor, windsurf, generic). No deps, no
  network, idempotent.
- [test/install.test.mjs](test/install.test.mjs): `node --test` coverage.
- [test/skill-content.test.mjs](test/skill-content.test.mjs): tripwire coverage
  — every proxy/global name must still appear somewhere in the skill content.

## Manual adapters

- [adapters/claude-code](adapters/claude-code/): Claude Code plugin form, plus
  a `/new-opal-script` command that scaffolds a new script file.
- [adapters/codex/AGENTS.md](adapters/codex/AGENTS.md)
- [adapters/gemini/GEMINI.md](adapters/gemini/GEMINI.md)
- [adapters/copilot/.github/copilot-instructions.md](adapters/copilot/.github/copilot-instructions.md)

## Project

- [README.md](README.md): what this is and how to install per assistant.
- [CLAUDE.md](CLAUDE.md): mental model and rules for changing this repo.
- [SECURITY.md](SECURITY.md): the script sandbox model and reporting.
- [CONTRIBUTING.md](CONTRIBUTING.md): how to edit the skill and regenerate adapters.

## Key facts

- Opal scripts are sandboxed `.js` files in `opal/scripts`: default-deny host
  access (`HostAccess.EXPLICIT`), no `Java.type`, no filesystem. Community
  scripts are also quarantined to `opal/scripts/pending` behind "Trust & run".
- Always build colors with `renderer.color(r, g, b[, a])`; never raw
  `0xAARRGGBB` literals.
- Only the API documented in `reference.md` and `reference/*.md` exists — do
  not invent methods. A member without `@HostAccess.Export` does not exist.
- Two silent traps: `mc.player`/`mc.world` do not exist (call `mc.getPlayer()`/
  `mc.getWorld()`), and collections are `ScriptList` (`size()`/`get(i)`), never
  arrays. Both read `undefined` rather than throwing.
